/** Per experiencias.md: every character starts with 2 Experiências at +2, growing later via Patrono ranks. */
export const STARTING_EXPERIENCE_COUNT = 2;
export const STARTING_BONUS = 2;

/** Soft cap noted in the source ("never more than 5 at once") — not enforced as a hard block on the Sheet. */
export const MAX_EXPERIENCES = 5;
